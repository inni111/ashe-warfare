# 交付AI提示词 · v4.3批次（COD经典地图重做 / Pick10军械库 / 枪械配件 / 投掷手模）

> 用法：把下面分隔线内全部内容原样复制给交付AI。交货后由总指挥安检，不要自己粘进游戏文件。

---

你是本次任务的代码交付AI。继续修改一个已可玩的COD粉丝致敬FPS（单文件cod.html，Three.js 0.160 CDN，WebAudio程序化音效）。上一批已交付：品牌UI（使命召唤：灰烬战争）、破片手雷G/闪光弹T（Nades对象/NADE_CFG）、检视F（Weapon.insA）、军械库（ArmoryUI/PERK_DEFS/MUZ_DEFS/ARM）、按键重绑（SET.bind/Menu.captureKey）。本批4个任务。你只交代码。

## 项目现状与定位方式

- 行号会漂移，定位靠名字：World/MAPS(rustout/nightport/dustout)/AudioSys/Music/Weapon/Bot/Game/ArmoryUI/Nades/SET/DEF_SET/NADE_CFG/PERK_DEFS/MUZ_DEFS/ARM
- 现有地图API：W.box(x,z,sx,sy,sz,mat,opt)造箱体、W.barrel/W.sand/W.rock/W.stack/W.building/W.car/W.tower等；MAPS每张图含spawns(ally/axis各6)/wps(路径点数组)/build(W)函数/雾光配色参数
- 玩家碰撞：World.collidesAt(x,z,r)圆柱检测；跳跃JUMP_V、重力GRAV；玩家目前只能走地面，**不能站上箱顶**

## 任务A：可站上掩体 + 三张COD经典布局新图

**A1 掩体顶部站立（先做，地图依赖它）**：
- 实现玩家跳上/走上低掩体（高≤1.3m的box可直接走上：碰撞检测时若玩家脚部高于箱顶-0.4则视为可跨越）；跳跃下落到箱顶时站在顶面（脚部高度=箱顶），走出边缘正常下落
- 修改点集中在World碰撞与playerUpdate落地逻辑，禁止破坏现有墙面阻挡；Bot不需要会跳，但Bot路径点可布置在地面层
- 自查：跳上集装箱顶、从顶走下、贴墙跳不穿墙，三者都不崩

**A2 新增三张图进MAPS**（选图界面自动列出，命名/文案精确照抄）：
1. `shipment` 运输枢纽 SHIPMENT：40x40小图。四角各一组两层集装箱塔（stack现有API可叠层），中央两组十字交叉集装箱（一层高，可跳上），四条边各一个出入口缺口，桶和木箱散布。对称布局，spawns在东西两侧，wps覆盖全部巷道
2. `crash` 坠机现场 CRASH：60x60中图。中央用box拼一架坠毁直升机残骸（倾斜机身+断尾桨，高2-3m不可穿越，周围留环形通道），两侧各一排可站上的矮掩体与沙袋环形，四角木箱堆
3. `dome` 雷达站 DOME：50x50小图。中央圆顶建筑基座（方体叠层+顶部平台可站），四周环形沙袋+八个对称掩体位，四角桶堆
- 每图配色/雾/光参数参照rustout白天调（crash暖黄昏、dome正午亮），**严禁再做暗到看不清的图**；fogD不高于0.008，exp不低于1.1
- wps密度保证Bot不卡死：任意路径点之间直线可达或经邻点可达

## 任务B：Pick 10 军械库（抄COD9黑色行动2）

重做ArmoryUI为Pick10配装界面，替换现有"被动五选二+预设三选一"逻辑（PERK_DEFS保留可复用）：

- 总点数10点，底部点数条（已用x/10），超点时保存按钮禁用并显示所需点数
- 类别与点数：主武器M4A1免费自带；**副武器M1911半自动手枪（新增武器，1点）**；配件每件1点；被动技能每个1点（最多选3个）；致命装备破片手雷1点（不选则每命0颗）；战术装备闪光弹1点（不选则0颗）
- M4配件（每件1点，同类不叠加）：
  - 枪口：消焰器（散布-10%）/ 制退器（后坐-15%）
  - 弹匣：快速弹匣（换弹-20%）/ 扩容弹匣（30发→42发）
  - 握把：垂直握把（后坐-12%）/ 激光（腰射散布-20%）
  - 瞄镜：全息（ADS时FOV再-8，准星更清晰）/ 无
- M1911数值：半自动，伤害34（爆头x1.5），弹匣8发，换弹1.6秒，后坐大散布中，射速上限每秒5发。音效用AudioSys.pn/pt组合（参考现有shot的分层思路：高频噪声瞬态+中频噪声+低频正弦thump，gain整体比M4低15%）
- 配置存SET.loadout并localStorage持久化；训练场/TDM开局按配置生效；默认配置=无副武器+无配件+破片+闪光+无被动（正好10点内）
- UI沿用现有语言：近黑底#0b0c0e+米白#e8e4d8+琥珀#ff9e2c+大字距+零圆角，左侧类别列表右侧选项列表，选中项琥珀左边条
- HUD：带副武器时显示切换键（默认滚轮或数字2键，进按键重绑系统，act名"weapon"切枪），换枪有1秒收枪/举枪动画期间不能开火

## 任务C：投掷物手模动画

- 用BoxGeometry拼一只左手（手掌+五指简化块+深灰绿手套材质+小臂），挂载在相机下
- 按住G（Nades.pin=true）时：枪模向右下偏移，左手抬到胸前做拔销姿态，手雷本体（复用Nades.mesh的frag几何）出现在左手中，cook时手轻微抖动
- 松开投出瞬间：左臂快速前挥0.25秒，手雷从手的位置起算初速（替换现有从camera.position出手的逻辑，起点改为手的世界坐标）
- 闪光弹T同理用右手做短投动作（0.35秒挥臂）
- 死亡/换弹/开火/ADS时手模立即归位隐藏；检视动画期间手模隐藏
- 手模只在pin/投掷动画期间可见，平时完全隐藏

## 铁律（违反任何一条直接打回）

1. 只改cod.html一个文件；禁止新库/新文件/新网络资源
2. 新增顶层变量集中在脚本区顶部声明区（402行附近的任务新增声明区延续），先声明后使用
3. 输出纯代码，严禁markdown围栏/解释性英文/思考笔记混进文件
4. AudioSys/Music内部一行不许改，只许调用现有方法（内部已判空，调用前无需再判）
5. 改函数名必须全文检查残留旧引用；改captureKey等现有函数时保持签名不变
6. UI红线：禁emoji/禁彩色渐变/禁大于2px圆角/禁卡通字体；近黑底+米白+琥珀+军事大字距
7. 游戏性红线：禁止打断玩家的屏幕提示文字
8. 交付前必须自查回归清单：滑铲结束站立/R重部署/受击红弧/训练场全流程/TDM全流程到结算/设置与按键重绑持久化/TAB战绩板/原音效无变哑/三张旧图不崩/跳上箱顶不穿墙不塌陷

## 输出要求

输出修改后的完整cod.html单文件内容，不分段省略，不附加说明文字。
