# 交付AI提示词 · v4.2批次（品牌UI/投掷物/检视/配装被动）

> 用法：把下面分隔线内全部内容原样复制给交付AI。交货后由总指挥安检，不要自己粘进游戏文件。

---

你是本次任务的代码交付AI。你要修改一个已经可玩的第一人称射击游戏（致敬《使命召唤：现代战争2019》的粉丝二创），完成4个任务。你只交代码。

## 项目现状

- 单文件 cod.html（约2800行）：HTML+CSS+一个 script type=module，Three.js 0.160 走jsdelivr CDN，WebAudio程序化音效，无构建无外部素材
- 已有系统：主菜单/开机动画、训练场模式、6v6 TDM对峙、三张地图、M4A1全自动（后坐/散布/ADS/分段换弹）、滑铲冲刺蹲跳、COD式HUD（hitmarker/击杀feed/+100飘分/受击方向弧/小地图/血条罗盘）、设置系统（灵敏度/FOV/音量/按键重绑/localStorage）、指针锁定+备用视角、TAB双模式记分板
- 行号会漂移，定位一律靠函数名：World/MAPS/AudioSys/Music/Weapon/playerUpdate/playerDamage/playerRespawn/Bot/Game(startRange/startTdm/endTdm/toMenu)/MapSel/SET/DEF_SET
- 音频层 AudioSys 和 Music 刚重构完，内部实现一行都不许动（见铁律4）

## 任务A：品牌UI全面对标MW2019

当前游戏名"灰烬战线 ASHEN FRONTLINE"，改成正式COD粉丝作品观感。文案精确如下，不要自由发挥：

1. title标签改为：使命召唤：灰烬战争 | CALL OF DUTY FAN TRIBUTE
2. 开机动画字幕 ASHEN STUDIO PRESENTS 改为 IN ASSOCIATION WITH ASHEN TASK FORCE
3. 主菜单大标题改 灰烬战争，下方小字 ASHEN WARFARE - A CALL OF DUTY FAN TRIBUTE
4. 主菜单列表改成MW2019左侧竖排结构（保留现有hover动效语言）：
   - 01 多人对战 MULTIPLAYER（进现有选图TDM流程）
   - 02 训练场 FIRING RANGE
   - 03 战役 CAMPAIGN（保持锁定，右侧小字 COMING SOON）
   - 04 军械库 ARMORY（解锁，接任务D）
   - 05 设置 SETTINGS
5. 页脚左：©2026 ASHEN STUDIO 非官方粉丝作品 与Activision无关；页脚右保留版本号
6. 结算画面：胜=VICTORY 任务完成；败=MISSION FAILED 任务失败；TDM记分板标题 TEAM DEATHMATCH
7. 击杀feed统一：普通 ELIMINATED +100，爆头 HEADSHOT +125

## 任务B：投掷物系统

致命装备破片手雷=G键，战术闪光弹=T键。必须接入现有按键重绑定系统并localStorage持久化。

破片手雷（每条命2颗）：
- 按住G拔销（枪模轻微抬起），松开投出；按住超2.5秒手中引爆（cook惩罚自伤全额）
- 初速约16m/s沿准星方向+少量上抛；重力下落，撞墙地面反弹（法向×-0.35，切向×0.7）
- 引信3.5秒从拔销起算；爆炸半径6m伤害100到0线性衰减，玩家自伤减半；范围内Bot死亡按玩家击杀走现有流程（feed+飘分）
- 爆炸视觉：现有Fx火花/尘土池大量spawn+PointLight闪光+相机震动
- 音效只许调 AudioSys.pn(...) 和 AudioSys.pt(...)。pn参数：freq/q/gain/dur/rate/pan/type/lp/rev；pt参数：type/f0/f1/gain/dur/when/rev。例：拔环 pn({freq:3000,q:1,gain:.12,dur:.05})；爆炸 pt({f0:60,f1:28,gain:.5,dur:.6,rev:.5}) 加 pn({type:"lowpass",freq:250,gain:.4,dur:.5,rate:.4,rev:.5})
- HUD右下弹药上方显示剩余数，手雷轮廓纯CSS线条画，严禁emoji

闪光弹（每条命1颗）：
- 同样物理，引信1.6秒落地段即爆；爆点9m内且视线可达时玩家全屏白幕1.5秒渐退（fixed div opacity动画）；Bot被闪停火+随机游走2秒
- 数量重生重置，新对局重置

## 任务C：武器检视

- 默认F键可重绑，仅地面、非换弹、非ADS、非滑铲可触发
- 基于现有武器模型做2.5秒动画：向屏幕中央收拢、绕纵轴缓慢旋转展示、回位；纯表现不改数值
- 移动/开火/跳跃/换弹任一发生立即打断回位；触发时一声轻金属摩擦音（AudioSys.pn实现）

## 任务D：军械库（配装+被动技能）

主菜单进入，UI沿用现有语言（近黑底+米白字+琥珀强调+大字距，无圆角无渐变）：

1. 被动技能五选二：
   - 轻装疾行 LIGHTWEIGHT 移速+8%
   - 快手 FAST HANDS 换弹时间-30%
   - 强韧 TOUGHENED 受击镜头抖动-50%
   - 死寂 DEAD SILENCE 自身脚步音量-70%
   - 警觉 ALERT 小地图显示开火敌人红点
2. M4配件预设三选一：CQB（后坐-20%/散布+15%/ADS快20%）；BALANCED默认；PRECISION（散布-25%/ADS慢15%/后坐+10%）。只改数值不改手感逻辑
3. 配置存localStorage（沿用SET持久化机制），训练场和TDM开局都生效

## 铁律（违反任何一条直接打回）

1. 只改 cod.html 一个文件；禁止引入任何新库/新文件/新网络资源；Three.js 0.160 CDN已有
2. 所有新增顶层变量必须在脚本区顶部工具函数区之后集中声明，不许在使用后才声明（历史事故：let T 和 rc 声明丢失导致全崩）
3. 输出必须是纯代码文件内容。严禁markdown围栏、解释性英文句子、思考笔记混进代码（历史事故：笔记泄漏导致启动崩溃）
4. AudioSys/Music 对象内部一行不许改。只许调用现有方法：AudioSys.pn(obj)、AudioSys.pt(obj)、AudioSys.hitmark/flesh/ui/tick/banner/trans/clack/surface/step/slide/hurt/heart/botShot/botDie/death/killBlip/shot/empty/reloadSeq/boomTick/resume。这些方法内部已做空值守卫，调用前无需再判
5. 函数改名必须全文检查残留旧引用（历史事故：fireCycle缺dt参数导致对局卡死）
6. UI红线：禁emoji图标、禁彩色渐变按钮、禁大于2px圆角、禁卡通字体、禁居中式布局；近黑底#0b0c0e+米白字#e8e4d8+琥珀强调#ff9e2c+军事大字距
7. 游戏性红线：禁止任何打断玩家操作的屏幕文字提示
8. 新功能用最小侵入方式接入：列出你改动的每个接入点函数名

## 回归清单（交货前自查）

滑铲结束自动站立；R键重部署；受击方向红弧正常；训练场全流程；TDM全流程到结算；设置与按键重绑localStorage持久化；TAB两种战绩板正常；原音效无缺失变哑。

## 输出要求

输出修改后的完整cod.html单文件内容，不要分段省略，不要附加说明文字。
