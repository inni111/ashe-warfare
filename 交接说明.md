# COD游戏项目交接说明

> 最后更新：V5.1（2026-08-29，敞亮质感一期：蓝天调色+程序化贴图+删弹药环+BGM+亮度滑条）。新会话接手前先读完本文档，再 `git log --oneline` 看提交历史。

## V5.1 改动速记（2026-08-29）

1. **删准星弹药指示器**：弹药信息只看右下角面板（30|120+装填条），准星区干净。
2. **8 图大气重调为正午晴朗蓝天**：全部在 MAPS 数据里（fogC/sunC/sunP/sk三段色/exp/hemi/ambient），太阳抬高、去黄雾、曝光 1.3+、环境光两轮提亮——"暗黄"从此是数据问题，改 MAPS 即可。
3. **程序化贴图第一批**：concTex/concDarkTex/brickTex/woodTex（makeTex 生成，256px 可平铺），matConc/matConcDark/matWood/rustout 砖楼已接线；**World.box 新增世界尺寸 UV**（1 格贴图=K 米，K 存在 tex.userData.K），任意大小墙面纹理密度一致。集装箱条纹 stripesTex 升级为立体波纹。
4. **对局 BGM 可闻**：applyVol 游戏模式乘数 0.26→0.6，schedule() game 分支加了太鼓+军鼓+闭镲+根音低音推进。
5. **设置新增"画面亮度"滑条**（SET.bright，70-150%，实时改 toneMappingExposure=图曝光×bright）。
6. **更新流程已简化**：remote origin 已配，gh 已登录。改完 index.html → 双击 sync-cod.bat → `git -c http.curloptResolve="github.com:443:140.82.114.3" push origin master:main`（IP 挂了就换 140.82.112.3 / 20.205.243.166）→ Pages 自动构建（`gh api repos/inni111/ashe-warfare/pages/builds/latest` 查状态）。不再需要 contents API 和 token。

## V5.0 改动速记（2026-08-28）

1. **M4A1 枪模重制**：分段机匣/抛壳口/M-LOK 护木散热槽/全长皮卡汀尼导轨（带护木齿）/准星柱+护圈/鸟笼消焰器（无配件时）/曲弹匣（两段）/贴腮枪托；AK 加导气管+表尺座+防尘盖条，MP5 加准星护罩+后照门+护木散热槽，SCAR 加导轨+贴腮。激光配件起点坐标从 x0.043 改 x0.032（updateLaser 同步改）。
2. **新增 World.block()**：多层建筑生成器（每层窗带为真实洞口，子弹可穿过；楼板；女儿墙；opt.door 一层开门可进室内；opt.stairs 外置双折梯可上楼顶）+ W.jersey() 隔离墩。rustout 全图重做（中央大街+三栋砖楼+厂房+集装箱阵），crash/crossfire 实心方块楼换成 block()，dustout 加两栋营房+第二座瞭望塔。
3. **全本地化**：three.module.js 已替换为官方 min 版（1.21MB→0.66MB，文件名不变）；Google Fonts 移除，改 fonts/ 下 6 个 woff2（Big Shoulders Display 500/700/800 + Rajdhani 500/600/700）——**现在完全离线可玩**。新增 sync-cod.bat（index.html→cod.html 一键同步，改完 index 必跑）。
4. **性能**：激光 raycast 降频到每 3 帧一次；HUD 四个 2D canvas 降到 30fps 重绘；主菜单降到 pixelRatio 1 省 GPU（进对局恢复）；换图 dispose 补释放本图专属材质（_pm 标记，共享材质不动）。
5. 已验证：node --check 语法过、无头 Edge 零报错、浏览器实测 rustout/dustout 对局+开火+换弹正常、新枪模与新地图截图验收通过。**待用户实测**：手感（Esc 重锁/滑铲/受击弧）与 crash/crossfire/dustout 新布局的 bot 寻路体验。

## 接手后当前状态（v4.1.1待用户验收）

用户反馈三大音频问题，总指挥亲自修完（v4.1.1）：
1. **耳鸣根因**：shot()每次开枪新建一条shaperOut→混响器的永久连线且从不拆除，几百发后混响输入叠加成持续啸叫。已在buildFx建常驻send并删除per-shot sendRev。
2. **outPan死胡同**（重大）：原实现把master连进一个没有输出连接的StereoPanner并返回它，导致所有pn/pt路径音效（脚步/受击/心跳/bot开火/hitmarker/换弹等）全部静默，仅带rev参数的音效经混响侧链出声。已重写为sp.connect(master)。
3. **BGM不可闻**：原Music全是<150Hz次低频+一段1750Hz噪声swell（后者本身像耳鸣）。已重写为可听暗黑电子乐（kick/snare/hat/bass pluck/pad和弦进行），新增game模式低调战斗音乐层（Music.setMode("game")），菜单/对局音乐不再中断。
**下一步**：用户验收音频→交付AI做（品牌UI改版+投掷物+检视+配装被动）→总指挥安检→战役模式。

## 项目是什么

单文件HTML第一人称射击游戏，目标质感对标《使命召唤：现代战争2019》。用户的工作流：总指挥AI（你）写提示词 → 用户复制给另一个"交付AI"写代码 → 用户把产出粘贴进 `cod.html` → 总指挥AI验证/修bug → 用户试玩反馈。

- 技术栈：单个HTML文件 + Three.js 0.160（仓库内 three.module.js，已换官方 min 版）+ 原生WebAudio合成音效，无构建、无外部素材（字体在 fonts/ 本地 woff2，**联网为零依赖**）
- 运行方式：⚠️ 本地 ES module 导入在 file:// 下会被浏览器 CORS 拦截，**不能双击打开**；本地测试双击 `本地测试.bat`（起 127.0.0.1:8123）后访问 http://127.0.0.1:8123/index.html；分发时整个文件夹打包（含 three.module.js 与 fonts/），对方任意静态服务器（或 GitHub Pages）可跑
- 分发：发给同事单文件即可；如需离线版需把Three.js内嵌进文件（约1.5MB，尚未做）

## 当前功能状态（v4，均已验证可玩）

- 主菜单：开机动画、BGM（WebAudio程序化原创）、COD式UI
- 训练场模式：8个木靶 + 4个掩体bot
- 对峙模式（TDM）：6v6团队死斗（玩家+5友军bot vs 6敌bot），先到50杀或5分钟，TAB记分板，胜负结算
- 三张地图：锈带废城 rustout / 夜港 nightport / 灰沙哨站 dustout（`MAPS` 对象，含spawns/wps/配色）
- 手感：WASD+冲刺+滑铲（结束自动站立）+蹲跳、全自动M4A1（后坐/散布/ADS/分段换弹动画）
- COD式HUD：hitmarker、击杀信息流、+100飘分、受击方向红弧、小地图、低血渐晕
- 设置：灵敏度/FOV/音量/音乐音量滑条、按键重绑定、画质开关，localStorage持久化
- 指针锁定：带失败重试提示层 + 无锁备用视角模式

## 待办（按优先级）

1. **战役模式**：菜单里"战役"仍锁定。规格：任务简报界面（打字机效果）→ 3名友军AI随行 → 目标链（推进A点→清剿→B点防守60秒→撤离点绿烟）→ 字幕系统 → 结算画面（击杀/爆头/命中率/用时）。友军AI可复用Bot类改team。
2. 音效若用户仍嫌吵：由总指挥亲自合成采样以base64嵌入（交付AI做不好这个）
3. 离线单文件版（内嵌three.js）供分发
4. 更远：军械库/多武器、成就、Godot迁移（用户提过想要"真游戏"出exe，但已达成共识：先在HTML上做完内容）

## 代码地图（cod.html，约2670行）

- 250~300：工具函数、临时向量、`rc`（全局Raycaster，**必须在文件前部声明**，地图加载初始化就要用）
- 449~783：`World`（地图搭建/加载/掩体点/路径点/出生点）+ `MAPS` 三张图定义
- 660~892：`AudioSys`（全部音效合成）+ `Music`（菜单BGM调度器）
- 1050~1260：武器（`Weapon`，射击/换弹/ADS）
- 1170~1260：玩家（`playerUpdate`/`playerDamage`/`playerRespawn`）
- 1590~1620：`KILLER_PLAYER`、`boardOpen`、`setTargetsVisible`
- 1620~1975：`Bot` 类（updateRange训练场行为 / updateTdm对抗行为 / roam游走 / fireCycle开火 / fireAt弹道 / pickCover找掩体）
- 1980~2070：`rebuildShootTargets`、`fillBoard` 记分板、`HUD`
- 2370~2480：`Game`（startRange/startTdm/addKill/endTdm/toMenu）+ `MapSel` 选图界面
- 2530~2620：键盘/鼠标输入绑定、指针锁定（`Lock`/`View`/`Pause`/`Hint`）
- 2609：`let T=0`（全局时间，**历史上有次重写丢了这行直接全崩**）

## 铁律（每次AI交付后必查）

交付AI已多次犯以下错误，验证时按此清单：

1. 顶层变量声明丢失（`let T`、`rc` 事故）→ 跑"裸赋值扫描"
2. **思考笔记泄漏进代码**（```围栏+英文句子事故）→ `Select-String '^\s*```'`
3. 函数改名后函数体残留旧变量名（`fireCycle` 的dt事故）
4. 音频节点访问无空值守卫（`master.gain`、`bus.gain` 事故）→ 原则：音频坏了最多静音，绝不能崩游戏
5. `node --check` 语法检查 + 无头Edge实测（命令见下）
6. 回归清单：滑铲结束站立、R键重部署、受击方向弧、训练场、TDM全流程、设置持久化

## 验证方法（无头实测，不用等用户踩雷）

```powershell
# 语法检查
$c = Get-Content "cod.html" -Raw -Encoding UTF8
$s = $c.IndexOf('<script type="module">') + 23; $e = $c.LastIndexOf('</script>')
Set-Content "$env:TEMP\cod_check.mjs" ($c.Substring($s, $e - $s)) -Encoding UTF8
node --check "$env:TEMP\cod_check.mjs"

# 无头运行抓真实报错（看输出里的 Uncaught 行）
& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless --disable-gpu --no-first-run --user-data-dir="$env:TEMP\ep_随机后缀" --enable-logging=stderr --v=0 --virtual-time-budget=10000 --dump-dom "file:///D:/VOS_User/Documents/opencode%20workspace/3/COD/cod.html" 2>&1 | Select-String "Uncaught"
```

局限：无头只能验证启动，游戏内的点击路径（开TDM局、bot开火）覆盖不到，需要用户实测。已知局限下高风险区：Bot类的TDM分支、模式切换状态重置。

## Git（回滚系统，就在本文件夹）

- 每个验证过的版本一个commit；重大可玩版本打tag
- 用户说"回滚"= `git checkout <commit> -- cod.html`
- 交付AI的版本粘贴进来之前，确认当前工作区是干净的（已提交）

## 与用户的约定

- 中文交流，语气直接，不用敬语铺垫
- 分工：崩溃/bug由总指挥亲自修（快且准）；新功能内容由总指挥写提示词给交付AI，交货后总指挥安检
- UI审美红线：禁止emoji图标、彩色渐变按钮、大于2px圆角、卡通字体、居中式布局；对齐COD MW2019：近黑底+米白字+琥珀强调色+大字距军事风
- 游戏性红线：不许出现任何打断玩家的屏幕文字提示（如"冲刺时不能开枪"）
- 3文件夹是总指挥的辖区：COD/DETROIT/NFS各归各，根目录不放杂物，每个游戏文件夹自己管自己的git
